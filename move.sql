create or replace procedure move(
									uname_input in users.uname%type,
									fpname_input in pages.pname%type,
									tpname_input in pages.pname%type
								)
	as

	-- Declarations
	

	begin

	--check if user is active
	--check if link exists
	--update user.pname to tpname_input


	end;
	/